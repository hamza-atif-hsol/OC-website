import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Digital Client Onboarding - One Constellation",
  description: "Streamline and automate your customer onboarding process with One Constellation's digital solutions.",
}

export default function DigitalClientOnboardingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
